import React, {Component, ReactPropTypes} from 'react';
import './index.css';

export default class Chart extends Component {
    constructor(props) {
        super(props)
        this.state = {
            initialized: false
        }
    }

    componentDidMount() {

    }
}